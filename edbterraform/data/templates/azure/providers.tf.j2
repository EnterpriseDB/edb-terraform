provider "azurerm" {
  features {}
}

{% for region in regions.keys() %}
{%   set region_ = region | replace('-', '_') %}
provider "azurerm" {
  features {
    resource_group {
      prevent_deletion_if_contains_resources = false
    }
  }
  alias = "{{ region_ }}"
}

{% endfor %}
