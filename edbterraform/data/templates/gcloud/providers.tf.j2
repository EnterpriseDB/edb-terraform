provider "google" {
}

{% for region in regions.keys() %}
{%   set region_ = region | replace('-', '_') %}
provider "google" {
  region = "{{ region }}"
  alias = "{{ region_ }}"
}

provider "google-beta" {
  region = "{{ region }}"
  alias = "{{ region_ }}"
}

{% endfor %}